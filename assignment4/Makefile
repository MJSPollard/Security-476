# build and run the file
run:
	@gcc -g -o ScannerFinder ScannerFinder.c -lpcap \
	&& ./ScannerFinder lbl-internal.20041004-1305.port002.dump.pcap

# remove generated files
clean:
	@rm -rf ScannerFinder